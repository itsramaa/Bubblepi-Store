{
  "buildCommand": "npx prisma migrate resolve --name 20260720000003_fix_enum --applied || true && npx prisma db push --accept-data-loss --force-reset && npx prisma generate && pnpm build",
  "installCommand": "pnpm install",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/cron/daily-report",
      "schedule": "0 7 * * *"
    }
  ]
}