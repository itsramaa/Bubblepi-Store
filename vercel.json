{
  "buildCommand": "pnpm prisma migrate deploy --skip-exit-code || true && pnpm prisma db push --accept-data-loss && pnpm prisma generate && pnpm build",
  "installCommand": "pnpm install",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/cron/daily-report",
      "schedule": "0 7 * * *"
    }
  ]
}